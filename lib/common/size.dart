part of 'common.dart';

const double defaultPadding = 16.0;
spacerHeight(double height) => SizedBox(height: defaultPadding * height);
spacerWidth(double width) => SizedBox(width: defaultPadding * width);
